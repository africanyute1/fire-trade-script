# ---------------------------------------------------------------
# SECRETS - never commit these. The API key belongs in GitHub
# Secrets (Settings -> Secrets and variables -> Actions), not here.
# ---------------------------------------------------------------
.env
*.env
!.env.example
secrets/
credentials*
*.key
*.pem
apikey*
api_key*

# ---------------------------------------------------------------
# PRIVATE FINANCIAL DATA - this repo is PUBLIC so the report can
# read it without authentication. Public market prices are fine.
# Your holdings, positions and account values are NOT.
# ---------------------------------------------------------------
holdings*
positions*
portfolio*
statements/
*_private.*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# OS
.DS_Store
Thumbs.db
desktop.ini
